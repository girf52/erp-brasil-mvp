# Projeto: SaaS ERP Vertical

**SaaS de ERP vertical** — um módulo por segmento, multi-tenant, NF-e via gateway. O grupo econômico do Raphael é o cliente piloto.

## Status: EM CONSTRUÇÃO

O gatilho (maturidade do MMX Gestão) foi acionado em **01/08/2026**. Existe código rodando, publicado e testado. O planejamento já foi feito — a planta está em `sistema/documentacao/` (51 documentos, comece pelo `INDICE_MESTRE.md` e pelo `PLANO_MESTRE_CONSTRUCAO.md`, que é o dono único de prazo, preço e ordem).

- **Código:** `sistema/` · repo privado `girf52/saas-erp-vertical`, branch **`main`**
- **Nuvem:** Supabase `ucflgecidugtolzplojg` (sa-east-1) · Vercel **`saas-erp-vertical-psi.vercel.app`** · CI no GitHub Actions
- **A Vercel mora na conta pessoal** (`garcia.couto90@gmail.com`), a mesma do GitHub `girf52` — é o que faz o deploy automático a cada push funcionar. O projeto no time `girf` ficou para trás: plano Hobby não faz colaboração em repositório privado, então todo commit era recusado como "autor externo". Voltar para o time só quando houver Pro
- **Estado em 05/08/2026:** 39 migrações, 513 testes verdes. **Onda 1 fechada no que não depende de contrato com terceiro**: pessoa → produtos/grade → estoque → vendas → categoria financeira/DRE → financeiro (títulos) → compras → OS/serviços → crediário → esqueleto fiscal → regime tributário (dual, com Simples híbrido) → conciliação bancária → relatórios/exportação → entrada de nota inteligente → migração assistida → GED
- **Travado por relógio externo (não por código):** emissão real de nota (contrato de gateway + certificado A1 + credenciamento SEFAZ), conteúdo tributário de presumido/real, BankingGateway (middleware bancário), TEF por adquirente
- **Estado em 06/08/2026:** 45 migrações, 533 testes verdes. **Telas auditadas** — 20 das 22 rotas percorridas com dado real; ficaram de fora só os três passos intermediários de `entrar`, que exigem segundo fator em curso. Oito defeitos achados e corrigidos, três viraram guarda (`mensagens`, `revogacao`, `relatorios_catalogo`)
- **Telas (05/08/2026):** o núcleo tem interface — caixa de entrada, cadastros (pessoa/produto/migração), vender, comprar (XML), financeiro (títulos/baixa/contas) e relatórios com exportação. 23 rotas. Tudo renderizado no servidor, formulários que postam, zero acesso ao banco pelo navegador
- **Estado em 07/08/2026 — Onda 2 construída:** 58 migrações, 619 testes verdes, 29 rotas, 10 seções no menu. Semana 0 (guarda ampliada) → produção/corte → expedição → CRM → PDV online → motor de nesting. Tudo no ar, semeado na nuvem e **percorrido com dado real** — o PDV foi testado de ponta a ponta pelo dono: abrir caixa → venda → troco → concluir
- **Segundo fator não vale em tenant de demonstração** (07/08/2026). A regra segue inteira para cliente real; o escopo é que mudou. Foi consequência de dar todas as capacidades ao papel da demonstração — **capacidade carrega exigência junto**, e três delas disparam MFA. Reverter é apagar o `and not t.demonstracao` em `interno.exige_mfa`
- **Tela não lê `interno.usuario` direto** — a tabela lista todas as pessoas de todos os clientes e não tem `tenant_id` para a RLS filtrar. Use `interno.quem_sou()` ou `interno.sessao_aberta()`. A regra existia só num comentário do `layout.tsx` e foi violada pela tela do PDV a um arquivo de distância; agora `testes/telas.test.ts` quebra o build
- **Estado em 07/08/2026 — Fase 9 fechada (contabilidade):** 61 migrações, 661 testes verdes, 33 rotas. Escrituração (partidas dobradas conferidas pelo banco), o ciclo (**provisório se cancela e some; definitivo se ESTORNA**, com espelho de sinal invertido, e os dois ficam), o motor (`contabilizar()` idempotente por origem, o lançamento nasce do documento), competência que recusa fechar com rascunho dentro, balancete/razão/DRE contábil, e o **painel do contador** — a segunda travessia de tenant deliberada, pelo mesmo consentimento do login, devolvendo contagens e nunca valores
- **`trocar_empresa` existe** (07/08/2026): trocar de empresa sem sair. O seletor de empresa vive numa pré-sessão do login, então quem já está dentro não o alcança. A função emite selo novo pelo MESMO caminho — identidade de `current_usuario()`, direito conferido por `emitir_sessao_usuario`
- **Estado em 08/08/2026 — a FRONTEIRA DO TENANT fechada (81 → 0):** 67 migrações, 739 testes verdes. A armadilha 4 era sistêmica: 81 funções `definer` alcançavam linha por id sem amarrar ao contexto — o sistema inteiro tinha **uma** protegida (`revogar_consentimento`). Sete sondas em cinco módulos entregaram sete de sete (limite de crédito, score de bureau, margem do concorrente, ficha do cliente, e ESCRITA: cancelar título e resolver caso de outro). Curado em seis ondas: financeiro (7) → crediário/preço/CRM (12) → GED/fiscal (7) → venda/PDV (13) → expedição/produção (21) → o resto (21)
- **O padrão é `interno.exigir_da_linha('<tabela>', <p_id>)` no TOPO da função**, ao lado do `exigir` que já estava lá. Vai na entrada, não em cada `where`: `baixar_titulo` toca o título em quatro comandos, e guarda na entrada cobre inclusive o que for acrescentado depois. **A coluna é escolhida pelo helper, nunca pelo chamador** — senão alguém confere tenant onde dava para conferir empresa, e um grupo com duas empresas teria o título de uma alcançável de dentro da outra. Sem contexto, RECUSA. Para lista em jsonb (`renegociar`, `conciliar_manual`), a guarda roda numa passada ANTES de qualquer escrita
- **Foi retirada a saída "tirar o BYPASSRLS do caminho"** depois de medir: as funções definer precisam escrever as colunas derivadas, que são justamente as revogadas da aplicação. Aquela saída obrigaria a reescrever a guarda de colunas derivadas ao mesmo tempo — e fazer as duas de uma vez é como se perde as duas
- **`testes/fronteira.test.ts` trava a reincidência** por igualdade exata, como `capacidade_exige_mfa`. A lista `PENDENTES` continua lá **vazia, de propósito**: não é mais dívida, é o escape hatch. Função definer nova que alcança linha por id quebra o build com a instrução de como consertar. `scripts/medir-fronteira.ts` mede do catálogo do Postgres, não da leitura dos arquivos
- **Estado em 08/08/2026 — Folha B-1 fechada:** 74 migrações, 788 testes verdes, 36 rotas. Colaborador é **papel de pessoa**, não segundo cadastro. **Contrato versionado**: reajuste é linha nova, nunca update — a folha de março precisa continuar achando o salário de março; `exclude using gist` impede duas vigências, e `salario_cents` está fora do grant de update. Rubricas com incidências parametrizadas (salário-família é reembolso do INSS: não incide em nada). Fechar a folha faz **três coisas juntas** — congela, gera título a pagar por colaborador e contabiliza
- **O motor da folha é HÍBRIDO e PURO** (decisão do dono): `lib/folha.ts` com `motorDeFolha()`, próprio como adapter padrão — mesmo desenho do `nesting`. O seam é `preparar_folha()` → motor em TS → `registrar_folha()`, igual ao `registrar_plano_corte`, inclusive o campo `motor`. Puro porque valor de folha é derivado (a aplicação não escreve, quem escreve é a função definer) e porque cálculo de imposto precisa de teste com entrada conhecida e saída conferida à mão — os 20 testes têm a conta aberta no arquivo
- **O REDUTOR da Lei 15.270/2025 é a armadilha da folha de 2026.** Ela não mudou faixa nenhuma: acrescentou um PASSO ao algoritmo, que zera o IR até R$ 5.000 e decresce até R$ 7.350 (`978,62 − 0,133145 × rendimento`). Um motor no modelo antigo — faixa + parcela a deduzir — calcula errado para quase todo assalariado do país, e o erro é silencioso. Por isso o redutor é linha versionada PRÓPRIA, não embutida nas faixas
- **Tabela de imposto só entra com a norma citada em `fonte`**, nunca por dedução do ano anterior, e `preparar_folha` RECUSA competência sem tabela vigente. INSS 2026: Portaria Interministerial MPS/MF 13 de 09/01/2026 — mínimo 1.621,00, teto 8.475,55. **Veio de transcrição: o PDF oficial não abre como texto, então o contador ainda precisa conferir as quatro faixas antes de folha real**
- **O Fator R deixou de ser digitado:** `folha_12m` alimenta `fator_r_automatico`. Só folha FECHADA conta — rascunho faria o enquadramento no Simples oscilar a cada recálculo do RH
- **Backup automático** em `.github/workflows/backup.yml` — diário às 03:00 BRT, mais `workflow_dispatch`. Extrai → cifra → envia ao B2 → **restaura o arquivo recém-gerado** conferindo tabela por tabela. Falha ANTES de extrair se faltar segredo, porque no runner um arquivo sem B2 morre com o disco e a execução terminaria verde sem backup nenhum. **Pendente do dono:** cadastrar `DATABASE_URL`, `DATABASE_PASSWORD`, `BACKUP_SENHA`, `B2_KEY_ID`, `B2_APP_KEY` em Settings → Secrets
- **A senha do backup é do dono, não do agente.** Arquivo cifrado com senha que só o agente conhece não é backup — é arquivo que o dono não abre. Já produziu lixo no bucket uma vez, num teste meu
- **Estado em 09/08/2026 — as SEIS personas têm tela:** 819 testes verdes, 38 rotas. O dono cobrou que o layout e a usabilidade não eram o combinado, e estava certo: eu tinha 788 testes de correção e ZERO de usabilidade. Redesenhadas por persona — balcão (PDV), fila do cortador, **meu dia** (vendedor, feed de pendências e nunca listagem com filtro), **painel** (dono, um número e o que ele significa), **caixa de entrada** (gerente, dois botões e resolve dali) e **carteira** (contador, ordenada por urgência). `testes/ux_balcao.test.ts` é o gate, com as leis que têm forma objetiva
- **`interno.papel_de_tela()` decide a porta, e é o BANCO que decide** — seis papéis. Não é autorização: quem barra continua sendo `interno.exigir`. A ordem das perguntas é o desenho, e o **contador vem primeiro** porque a carteira dele atravessa empresas
- **Rota que é tela E roteador ao mesmo tempo quebra no dia em que alguém é redirecionado para longe dela.** A caixa de entrada era `/` e também decidia o destino de cada papel; quando o dono ganhou `/painel`, o item "Caixa de entrada" do menu apontava para `/`, que o devolvia ao painel — ele clicava e não saía do lugar. Agora `/` **só roteia** e cada tela inicial tem endereço próprio (`/avisos`). Nenhum teste tinha percebido, porque nenhum teste clica
- **`acao_rotulo`/`acao_destino` de `notificacao` nunca foram preenchidos por ninguém** desde 03/08 — toda notificação apontava o problema e não levava a lugar nenhum. Enquanto o banco não manda, `lib/aviso.ts` traduz pelo `tipo`, e mora em `lib/` porque DUAS telas mostram aviso: com o mapa dentro de uma, a outra fica com o defeito a um arquivo de distância
- **Terminação de linha derruba guarda calada.** Em JavaScript `.` não casa `\r`, então `//.*$` deixa de reconhecer comentário assim que o arquivo é salvo em CRLF — e `testes/telas.test.ts` passou a acusar a própria frase que documenta a regra. Guarda que lê arquivo tira o `\r` antes
- **Estado em 09/08/2026 — as 21 telas de módulo redesenhadas:** 863 testes verdes, 39 rotas. Trilha ("rastreio de encomenda") em vender/comprar/produção/expedição, veredito primeiro no financeiro, cartões de app de banco em relatórios, `<details className="porque">` da Lei 9 onde há número calculado
- **`lib/rotulo.ts` é o dicionário dos 77 enums** — um arquivo só, e não um mapa por tela: a mesma venda aparece em cinco telas, e cinco mapas divergem em uma semana. `estadoDoTitulo()` sabe o que a tabela não sabe — `baixado` é *recebido* numa conta a receber e *pago* numa a pagar, e `aberto` vencido é ATRASADO, estado que não está no enum porque quem o cria é o relógio
- **`testes/rotulo.test.ts` lê o CATÁLOGO do Postgres**, não os arquivos: migração que criar valor de enum novo quebra o build dizendo qual falta traduzir. E a guarda de tela reprova `{x.status}` interpolado em JSX, em toda tela. A medida inicial foi 16 telas / 27 ocorrências — depurada duas vezes antes de eu confiar nela (acusava `key={e.forma}` e `className={...}`, e depois adivinhava pelo nome da coluna, acusando `{o.motivo_cancelamento}`, que é frase digitada por gente)
- **A barra universal (`/buscar`) é a navegação primária** (Lei 6, que chama menu profundo de bug de design). Sete famílias numa caixa. Ela ACHA; não EXECUTA — isso depende do LLMGateway, e a tela diz isso
- **Texto de tela escrito pelo BANCO é uma classe que nenhuma guarda alcança.** `rotulo()` traduz enum, a guarda confere JSX — mas string literal gravada por função definer em coluna de texto livre é, para a tela, um dado como outro qualquer. Cinco ocorrências curadas na origem em 09/08/2026: `baixar_titulo` gravava "Baixa de titulo" na descrição de todo movimento; `eventos_nao_contabilizados` devolvia motivos sem acento; `semear_funil_padrao` criava etapas que são cabeçalho de coluna no CRM; o catálogo `interno.relatorio` inteiro (13 nomes, 13 descrições) numa tabela que existe só para ser lida; e "Estorno do lancamento N", que abre a linha de todo estorno no Diário
- **Acentuar nome que é chave de consulta quebra o que busca por ele.** As etapas do funil derrubaram `scripts/cenario.ts` e `testes/crm.test.ts` de uma vez — ali o nome não era rótulo, era chave. Os três lados mudam juntos
- **O MENU SAI DA CAPACIDADE, o PAPEL só decide a porta** (10/08/2026). Filtrar o menu por `papel_de_tela()` apagou o ERP inteiro para o dono: a conta dele está ligada ao escritório contábil do grupo, `sou_contador()` devolvia verdadeiro, o papel virava "contador" e sobrava um item de menu. O erro é conceitual e estava escrito no comentário da própria função — ela **não é autorização**. Agora `interno.minhas_capacidades()` monta o menu (papel na empresa + consentimento do contador + chave de API), e `papel_de_tela()` pergunta `administracao.usuarios` ANTES de `sou_contador()`: quem administra a empresa em que está é dono dela, mesmo assinando a contabilidade do grupo. Um caixa puro continua vendo só o PDV, agora por derivação e não por lista escrita à mão
- **Atalho de persona (`Minha fila`, `Meu dia`) sai do PAPEL; módulo sai da CAPACIDADE.** O dono tem `producao.apontar` como tem tudo, e a fila do cortador no menu dele é ruído. A fragilidade que sobra é de outro tamanho: no pior caso falta um atalho e a tela continua alcançável pelo módulo
- **A barra tinha 128px grudados no topo** porque o nome e o "sair" na mesma linha deixavam 909px para uma lista que precisa de 1120. Empilhada, cabe em 68px; no celular a fileira desliza em vez de quebrar (eram 210px de 812, um quarto do aparelho)
- **Estado em 10/08/2026 — as quatro frentes que faltavam, fechadas:** 931 testes verdes, 80 migrações. GED versionado com tela e preview · Raio-X + site público · e-mail transacional · folha B-2
- **GED:** `anexo_versao` guarda a versão que SAIU; substituir exige motivo e **nunca apaga** — o arquivo antigo continua contando o prazo dele. `lib/guarda.ts` é o adapter de armazenamento (B2), com **chave separada da do backup**, porque a do backup é `Write Only` de propósito. Pendente do dono: `GED_B2_KEY_ID` e `GED_B2_APP_KEY`
- **`espaco_usado()` e `arquivos_a_expurgar()` liam `public.arquivo` SEM FILTRO** — definer com BYPASSRLS, enxergando o arquivo de todos os clientes, e a segunda devolvia `storage_ref`. Ficou dez dias invisível porque só havia um tenant com arquivo; apareceu quando a semeadura criou o terceiro documento. `testes/fronteira.test.ts` não pega esta forma (função sem id, que varre a tabela) — `testes/ged_vazamento.test.ts` pega
- **A raiz virou o SITE PÚBLICO**; o roteador de papel mudou para `/entrando`. Um SaaS cujo endereço principal exige senha não recebe quem chega pela primeira vez
- **O Raio-X não guarda nada, e o primeiro `'use client'` do projeto existe por causa disso** — não guardar e ter endereço são incompatíveis. As quatro alternativas estão descartadas por escrito em `raio-x/formulario.tsx`
- **E-mail: a autorização ENFILEIRA e volta.** Provedor fora do ar não pode virar nota não autorizada. Um gatilho em `documento_fiscal`, não uma chamada dentro de `autorizar_documento` — assim vale para o gateway quando ele existir. Desiste na quinta tentativa e vira pendência com botão. Motor com adapter; o padrão `registro` **não manda e diz que não mandou**
- **Folha B-2 — o que mais erra é a INCIDÊNCIA.** Aviso indenizado, férias indenizadas, abono pecuniário e multa do FGTS são verba indenizatória: não entram em base nenhuma. A 1ª parcela do 13º não desconta nada (o imposto incide uma vez, sobre o inteiro, na 2ª). O aviso indenizado **projeta o contrato** para 13º e férias. Férias vencidas são devidas até na justa causa
- **Estado em 10/08/2026 — os três itens de código do lote:** 990 testes verdes, 84 migrações, 42 rotas. Desfazer a baixa · régua de cobrança · inventário e contagem cíclica
- **DESFAZER A BAIXA (Lei 10) é ESPELHAR, nunca apagar.** `movimento_financeiro` é `interno.tabela_historico`, e isso não é obstáculo — é o desenho certo: extrato que apaga linha deixa de bater com o do banco. `baixa_id` agrupa as três linhas de uma baixa (principal, juros, desconto) e o desfazer reverte o GRUPO — desfazer só o principal deixaria os juros lançados sobre um título que voltou a dever tudo. **O título se reabre SOZINHO**, pelo gatilho que já existia: a amortização negativa faz a soma cair. Foi preciso trocar `amortizacao_cents >= 0` por `>= 0 or estorno_de is not null` — a restrição valia enquanto o título só andava para frente
- **RÉGUA DE COBRANÇA: ela chega DESLIGADA, e ligar pede confirmação escrita.** Régua que nasce ligada manda e-mail para a carteira inteira na primeira vez que o worker roda, e **e-mail entregue é a única coisa do sistema sem estorno**. Dois cuidados que sustentam o resto: (1) **só o degrau MAIS ALTO já vencido dispara** — ligar numa carteira com 40 dias de atraso mandaria os quatro degraus no mesmo minuto, e o cliente lê hostilidade, não sistema novo; depois do último, ela CALA, porque daí para frente é telefone; (2) **pagou, ela para** — título fora de `aberto`, ou amortização que SOMOU positivo nos últimos 7 dias. É a soma e não "existe movimento positivo": desfazer a baixa deixa a linha original e o espelho, e "existe positivo" daria uma semana de silêncio por um Pix que voltou
- **A origem da mensagem de cobrança é o DISPARO, não o degrau.** A caixa de saída é única por (tipo, origem, para) — o degrau como origem colaria os dois títulos do mesmo cliente e ele nunca saberia da segunda dívida. Por isso `cobranca_disparo` nasce ANTES da mensagem
- **INVENTÁRIO: o ajuste é uma DIFERENÇA, nunca um "passe a valer X".** Fechar escrevendo `saldo = contagem` apaga a venda feita durante a contagem — o número fica plausível e errado. O saldo do sistema é congelado no INSTANTE da contagem, e o fechamento grava um movimento com o delta; movimento posterior continua valendo. A recontagem é dirigida (só o que divergiu) e **a folha não mostra o saldo do sistema enquanto se conta** — quem vê "40" e conta 38 confere de novo até achar 40. Quem decide isso é o banco (`mostrar_saldo`), não a tela
- **`ajuste` de estoque virou `ajuste_saida` + `ajuste_entrada`** (10/08/2026). Com um tipo só o sinal era sempre −1, e toda SOBRA viraria falta. Renomear em vez de acrescentar porque `ajuste` tinha zero linhas — e `alter type ... add value` não pode ser usado na mesma transação, então o enum mora num arquivo de migração próprio. O acerto de contagem **não entra na média** de custo: a sobra já foi comprada um dia
- **`npm run worker` estava QUEBRADO e ninguém percebia** (achado em 10/08/2026). O processo morria carregando módulo, e a caixa de saída do e-mail nunca tinha sido esvaziada. 931 testes verdes não pegaram porque o Vitest tem transpilador próprio e aceita o que `node --experimental-strip-types` recusa. Duas formas: import relativo sem extensão (`'./banco'`), e **propriedade de parâmetro** (`constructor(private readonly x: T)`) — o strip-types APAGA tipos em vez de transformá-los, o modificador some e nada atribui `this.x`. `testes/worker_sobe.test.ts` SOBE o processo de verdade, então uma terceira forma é pega do mesmo jeito
- **Estado em 10/08/2026 — as pendências de credencial FECHADAS, e o worker deixou de depender da máquina do dono:** 999 testes verdes. Backup diário provado, GED escrevendo e lendo do B2, e-mail saindo pelo Resend com agendador próprio
- **BACKUP:** os cinco segredos entraram no GitHub. A execução manual provou o ciclo inteiro — 1020,9 KB, 129 tabelas, impressão digital confirmada pelo B2, e **restauração aprovada com as 129 contagens exatas**. Roda 03:00 BRT. A senha é do dono e não existe cópia: sem ela todo `.sql.enc` é lixo permanente
- **DUAS CHAVES DE B2, e a razão não é burocracia.** A do backup é `Write Only` de propósito — se vazar, sujam o bucket mas não leem o banco. O GED precisa LER, então tem chave própria (`GED_B2_KEY_ID`/`GED_B2_APP_KEY`), `Read and Write`, restrita ao bucket dos documentos. Apontar as duas para a mesma chave teria trocado a segurança do backup por uma variável a menos
- **`lib/b2.ts` descobre o bucket pela CHAVE**, não por variável: a chave restrita carrega o `bucketId`. Chave criada com "All buckets" é recusada — assim não existe o cenário "a chave é do bucket A e a variável aponta para o B"
- **A semeadura cria a ficha do documento e nunca envia o byte** — ela não passa por `lib/guarda.ts`. Os documentos semeados respondem `502 "O registro existe, mas o arquivo não voltou do armazenamento"`, que é o comportamento certo (recusa alto em vez de devolver página em branco), mas deixa dois documentos que não abrem na demonstração
- **E-MAIL: subdomínio (`envio.`), nunca o domínio raiz.** Verificar no raiz faz os registros de assinatura disputarem com os do provedor de e-mail humano, e o sintoma aparece no lugar errado — o e-mail pessoal do dono caindo em spam. Separa também reputação: cobrança marcada como spam não contamina o domínio com que se fala com as pessoas
- **O WORKER RODA PELO pg_cron do próprio banco** (10/08/2026), escolha do dono entre quatro opções. `pg_cron` → `pg_net` → `POST /api/fila` na Vercel. Grátis, granularidade de minutos, sem infraestrutura nova. O Hobby da Vercel limita cron a 1×/dia, e GitHub Actions consumiria ~1.500 dos 2.000 minutos livres
- **Duas cadências, e não uma:** correio a cada 5 min (quem comprou espera o XML) e régua às 09:00 e 15:00 BRT. Cobrar de cinco em cinco minutos não cobra mais, só varre 288 vezes por dia. E **nunca de madrugada** — cobrança que chega às 3h é lida como robô
- **O agendamento é SCRIPT, não migração** (`scripts/agendar-worker.ts`). Migração viajaria com o endereço do site: um `db reset` na máquina do dono passaria a disparar produção de 5 em 5 minutos, para sempre. E carregaria o segredo para o repositório. O script RECUSA rodar contra banco local. O segredo vai para `interno.segredo`, não para o texto do comando — `cron.job` é tabela legível
- **A régua é chamada FORÇADA pelo endpoint.** O relógio em memória de `lib/cobranca.ts` serve ao laço de 3 segundos do worker local; numa função sem servidor ele valeria ou não conforme o processo ser novo ou reaproveitado, e o mesmo agendamento rodaria por acaso
- **`net.http_post` é ASSÍNCRONO: `cron` marca "succeeded" só por ter enfileirado.** Site fora do ar, segredo trocado ou 500 aparecem apenas em `net._http_response` — o agendador continuaria verde enquanto nada acontece. `npm run conferir:nuvem` pergunta as duas tabelas; `npm run sondar:nuvem` acorda na mão pelo mesmo caminho do cron
- **A SEMEADURA SOBE O BYTE** (10/08/2026). Ela gravava a ficha do documento com `storage_ref` apontando para o nada, e o comentário dizia ser proposital — o motivo real era não haver credencial de GED. Com ela no ar, o que sobrava era uma demonstração com dois documentos que não abrem, indistinguíveis de defeito. Agora sobem pelo mesmo caminho da tela (conferir → guardar → registrar), o que exigiu **gerar um PDF que é mesmo um PDF**, porque `lib/arquivo.ts` confere os primeiros bytes. O caminho vem do hash: semear de novo sobrescreve o mesmo objeto em vez de acumular lixo no bucket. Sem credencial, avisa e registra só a ficha — semear não pode passar a exigir conta no B2
- **Guarda de comportamento mora em TESTE; a demonstração mostra o sistema funcionando.** O caminho de erro que o documento quebrado demonstrava virou `testes/arquivo_servir.test.ts`: 404 igual para "não existe" e "não é seu" (senão a rota vira confirmador de existência de documento alheio), 503 de quem instala, 502 de quem opera, XML como download e o 502 sem vazar o caminho no bucket
- **E-mail de demonstração é `delivered@resend.dev`.** Enquanto o motor era o `registro`, endereço inventado era inofensivo; com o Resend no ar, a nota da demonstração passou a ser ENVIADA para domínio que não existe, e bounce repetido queima a reputação do mesmo domínio que vai cobrar cliente de verdade. O endereço de teste do provedor aceita, marca entregue e não encosta em servidor nenhum — e é mais honesto na tela. **A régua continua desligada no tenant de demonstração**
- **PAINEL DO OPERADOR — a terceira travessia de tenant** (10/08/2026). O contador atravessa por consentimento, `trocar_empresa` dentro do grupo, o operador atravessa TODOS sem consentimento de ninguém. Três regras a contêm: a capacidade vem de `interno.operador` e **não** de `usuario_empresa`; devolve contrato e consumo e **nunca a linha de dentro** (teste varre o catálogo do Postgres e quebra se alguém acrescentar função de operador que leia venda, título ou pessoa); e tudo vai para a trilha em `travessia_tenant`. `scripts/promover-operador.ts` cria o primeiro — tela que cria operador precisaria de um operador para existir
- **`apagar_tenant` varria o operador** e foi preciso excetuá-lo: ele não tem vínculo com empresa por desenho, o que o tornava indistinguível de usuário órfão. Apagar UM cliente derrubava o operador de TODOS, em cascata. Quebrou a própria suíte no teste seguinte ao que apaga um tenant
- **Preço CONGELADO até as camadas pagas estarem integradas** (decisão do dono, 10/08/2026). O catálogo é editável em `/operador`, então o ajuste é de minutos e não de deploy. **O que falta para fechar:** mudar o preço muda a mensalidade de quem já assinou, na hora — falta `preco_travado_cents` (preço de fundador) e vigência no catálogo, pela mesma regra do contrato da folha ("reajuste é linha nova"). Não é urgente enquanto não há faturamento, que espera o BankingGateway
- **Layout novo + segmentação por ramo — PLANEJADO em 10/08, decidido e iniciado em 11/08/2026** (as decisões da leva estão mais abaixo). Dois protótipos aprovados como direção: três propostas comparadas e um navegável com o gesto do PDV generalizado — **grade de alvos grandes ao centro, painel de consequência fixo à direita, estado inequívoco no topo**. Decidido pelo dono: **o balcão abre em TELA CHEIA, sem trilho** (quem atende tem uma mão ocupada e alguém esperando), com a saída no topo e, no celular, total e "concluir" grudados embaixo enquanto a grade rola atrás
- **A gramática é o produto, não a aparência:** a intuição vem de todas as telas serem a MESMA tela com conteúdo diferente. Layout novo = Trilho (persona à esquerda) + barra de comando no topo + grade de alvos. O acento é o ouro mineral do *Amarelo Ornamental* do próprio catálogo, sobre ardósia fria — deliberadamente NÃO o azul-500 do Tailwind do sistema de referência
- **SEGMENTAÇÃO POR RAMO: são TRÊS EIXOS, e confundi-los é o erro clássico.** (1) **Capacidade** — o que a PESSOA pode, já existe em `minhas_capacidades()`; (2) **Módulo contratado** — o que a EMPRESA comprou, existe em `assinatura_item` e **NÃO filtra o menu hoje** (cliente sem PDV contratado continua vendo PDV — lacuna real, entra no plano com ou sem segmento); (3) **Segmento** — o que a empresa É, não existe nada
- **Segmento não é "quais módulos".** Ele muda vocabulário (chapa/talhão/sacaria — `lib/rotulo.ts` traduz enum, falta o dicionário de DOMÍNIO), padrões de cadastro (unidade, casas decimais, controla lote/série), qual tela é a porta, e o catálogo fiscal inicial. **Regra defendida: segmento NUNCA esconde o que a empresa contratou** — ele ordena, nomeia e pré-configura. Esconder por segmento cria o pior chamado que existe: "eu paguei e não aparece". Ocultação é sempre por módulo não contratado, que tem resposta comercial. Em uma linha: **menu = capacidade ∩ módulo contratado, ordenado e rotulado pelo segmento**
- **O ERP de referência do dono (`erp-frontend-alpha-azure`) é PROTÓTIPO DE INTERFACE** — duas rotas de navegação, dados fixos no código, sem multi-tenant nem emissão. Comparar função a função nos favoreceria sem ajudar a decidir. O que vale dele é o vocabulário e o gesto do varejo. **A lacuna do varejo tem de ser MEDIDA contra o código, não contra a memória** — hipóteses a conferir: código de barras na venda rápida, venda por peso, troca/devolução no balcão, sangria e suprimento, promoção por período, turnos de caixa, kit/combo. TEF e NFC-e em contingência entram na lista com etiqueta de "travado por contrato"
- **AS DECISÕES DA LEVA DOS TRÊS SEGMENTOS, fechadas em 11/08/2026** (medição contra o código antes, plano depois, execução por último). São **TRÊS segmentos simultâneos** — varejo, marmoraria e agro —, cada um "completo" no sentido de tudo que não depende de terceiro. Clientes que esperam: **Casa do Fazendeiro** (agro-varejo, 1 matriz + 2 filiais no mesmo grupo) e as marmorarias do grupo, que já operam
- **Segmento é da EMPRESA e o vocabulário troca junto com `trocar_empresa`.** A herança é de ESTRUTURA e de um nível só (`segmento.base`): agro herda varejo, marmoraria não herda nada, e quem não tem `base` cai no padrão do sistema. **Dado nunca cruza empresa nem segmento** — a herança vale para o catálogo em `interno`, jamais para linha de cliente. Composição por traços foi descartada: o operador passaria a montar segmento marcando caixinhas, e "você é agro" vira explicação longa na venda
- **Menu = capacidade ∩ módulo contratado, ordenado e rotulado pelo segmento.** `assinatura_item` passa a ser **por empresa** (hoje é do tenant, e por isso não sabe representar "a matriz tem PDV, a filial não"). A migração **contrata tudo que hoje aparece**, empresa por empresa: ninguém perde tela por efeito colateral de deploy. O cliente **pede** módulo pela tela e o operador contrata — autosserviço exigiria preço descongelado e cobrança, que espera o BankingGateway
- **Catálogo é do GRUPO, preço é por empresa quando preciso.** O `sku` continua único por tenant; quem varia é o preço, via `tabela_preco` (vigência + empresa + lista varejo/atacado/revenda), pela regra do contrato da folha: preço novo é linha nova. Catálogo por empresa foi descartado — o mesmo defensivo viraria três cadastros com três NCMs que ninguém garante iguais
- **Papel ganha a porta; o segmento só desempata.** `papel_de_tela()` continua mandando (caixa entra no balcão em qualquer ramo); o segmento decide quando o papel não é conclusivo — vendedor de marmoraria cai no orçamento, o de varejo no balcão. Inverter isso quebraria a Lei 1, que o dono já cobrou uma vez
- **O agro entra SEM o bloco regulatório** — receituário agronômico, produto controlado (MAPA/PF/Exército) e embalagem vazia ficam fora: o grupo já tem solução à parte rodando até integrarmos. **A nota de produtor rural fica DENTRO**: é fiscal, não é produto controlado, e sem ela o agro não registra a compra de quem planta
- **A LACUNA DO VAREJO, MEDIDA CONTRA O CÓDIGO em 11/08/2026** (não contra a memória, e o resultado desmentiu a memória em dois pontos). *Existe e é bom:* EAN com dígito verificador em `sku`, bipagem no PDV, sangria/suprimento como `movimento_financeiro`, terminal + sessão de caixa com uma aberta por terminal, e o motor de devolução nos dois sentidos com custo carimbado. *Não existe:* kit/combo, etiqueta, promoção, `tabela_preco`, comissão, turno
- **O que a medição achou e ninguém procurava: não há TELA para cadastrar `terminal` nem `conta_recebimento`.** Os dois só nascem em `scripts/cenario.ts` — o PDV inteiro está de pé sobre um script de demonstração, e cliente novo de varejo não abre o caixa. Pior: `finalizar_venda_balcao` manda o usuário para "Cadastros → contas", tela que não faz isso. **É bloqueante para onboarding**; se entrar cliente antes da fase, ela sobe na hora
- **Outros três da mesma medição:** `/vender` busca SKU **só por `codigo`** — bipar não funciona fora do balcão · `interno.unidade_medida.fracionavel` **nunca é lido por ninguém**, então 2,5 parafusos passa · e `movimentarCaixa` escolhe a categoria financeira sozinho (`order by codigo limit 1`), fazendo toda sangria do sistema cair numa categoria arbitrária que a DRE come calada
- **E na marmoraria, que "já opera": a Ordem de Serviço não tem tela nenhuma.** O enum tem os cinco tipos e o ciclo está desenhado no banco desde 04/08; em `app/`, `ordem_servico` só aparece na busca e na timeline do CRM. Medição e instalação são feitas fora do sistema. O módulo `servicos` está no catálogo a R$ 149/mês sem lugar onde ser usado
- **Comissão apurada no RECEBIMENTO, não na venda.** Só vira comissão o que entrou — resolve sozinho a venda cancelada, o título não pago e o crediário em doze vezes, e o gancho é a baixa do título, que já sabe espelhar quando se desfaz. O destino é escolhido no cadastro (título a pagar para autônomo, rubrica para CLT), o que **amarra a fase à folha mesmo para o autônomo** — decidido assim com essa informação na mesa
- **Escolhas menores da leva, todas com o porquê:** kit **explode na venda** e baixa componentes (saldo de kit seria um segundo lugar para divergir, e a nota precisa dos itens reais) · etiqueta em **PDF com gabarito**, sem dependência nova, com a térmica marcada · **código interno na faixa GS1 reservada** (prefixo 2), inclusive por chapa serializada, para bipar *aquela* chapa com o custo dela · **etiqueta de balança** lida pelo PDV com peso digitável para quem não a tem · devolução no balcão devolve **dinheiro da gaveta OU crédito**, o operador escolhe · transferência entre lojas **direta**, fechando o par de enums que existe sem ninguém usar · **turno de caixa fica de fora**
- **Travado por terceiro nesta leva:** NFC-e e NF-e reais · TEF e adquirente · NF de devolução referenciada · autosserviço de contratação (BankingGateway) · IA na barra de comando (fora por custo por uso, decisão do dono). E **travado por AMBIENTE, que é diferente de contrato**: balança acoplada e etiquetadora térmica dependem do mesmo aplicativo local que o PDV local-first vai exigir — entram juntos quando ele existir
- **A ORDEM, e as três correções que a revisão do próprio plano produziu:** segmento no banco → módulo por empresa → dicionário de domínio → **as três demonstrações** → layout (45 telas + as caronas) → destravar o varejo → preço → agro → devolução/troca → kit, etiqueta, código interno, peso → transferência → OS → comissão → revisão de fim de onda. As correções: (1) as demos **sobem para antes do layout**, porque a regra do projeto é semear movimento antes de conferir tela e eu conferiria a grade de alvos numa marmoraria só; (2) a tela de "pedir módulo" nasce **no layout novo**, não na fase do banco, senão nasce velha; (3) o **agro sobe** para logo depois do preço — deixá-lo por último atrasaria o cliente que espera sem motivo técnico
- **A demonstração passa a ser UM TENANT POR SEGMENTO.** Três cenários semeados com movimento, cada um com o vocabulário e os produtos do ramo. De quebra, é o cenário de três clientes que a regra de fronteira exige — "um cliente só no banco não prova isolamento"
- **F5 (preço) é a de maior risco do plano, não o layout.** O layout mexe em muito arquivo e em pouca regra; o preço muda de onde vem o número em venda, PDV e OS ao mesmo tempo, e número errado nasce calado
- **AS DECISÕES DE FORMA DE F6 A F12, fechadas em 11/08/2026** (doze cards, respondidos de uma vez para a execução seguir sem parar):
- **Devolução (F6):** tela NO BALCÃO e tela PRÓPRIA — são dois gestos diferentes, um de pé com o cliente esperando e outro sentado com a nota na mão. Parcial item a item e por quantidade: devolver um saco de três não pode obrigar a cancelar a venda inteira, porque a venda refeita perde número, data e nota
- **Kit (F7):** preço PRÓPRIO, e a diferença para a soma dos componentes vira desconto rateado pelo caminho que `aplicar_desconto_venda` já faz. Kit sem preço próprio não serve a combo promocional, que é o motivo de kit existir no varejo
- **DUAS ETIQUETAS, e são públicos diferentes** (F7): a de GÔNDOLA leva nome, preço e unidade — é para o cliente ler na prateleira; a de PRODUTO leva nome, código de barras e o identificador da peça — é para o leitor, e vai colada na mercadoria. Preço em etiqueta que fica na chapa envelhece junto com a chapa
- **Transferência (F8):** em Cadastros → Estoque, junto das contagens, e ela CARREGA O CUSTO. Transferir sem custo faria o destino receber mercadoria de graça e a origem perder margem — o CMV das duas sairia errado sem ninguém ter vendido nada
- **A OS cobra e consome (F9):** gera título a receber quando concluída, baixa o material que gastou, e amarra na venda que a originou (`venda_id` já existe na tabela desde 04/08). Sem isso ela registra o trabalho e deixa dinheiro e material fora do sistema — que é o trabalho manual que a tela deveria eliminar
- **A base da comissão é ESCOLHA DO CLIENTE** (F10): valor recebido ou percentual por produto, configurável. O dono de cada empresa decide — não é o sistema que impõe o modelo comercial
- **Vendedor OBRIGATÓRIO na venda, e o rateio é em PARTES IGUAIS** (F10). Mais de uma pessoa pode dividir; dois vendedores, metade para cada. Partes iguais e não percentual digitado: nada para errar na soma, e o caso de "atendi 90% e chamei o colega" não justifica um campo a mais em toda venda
- **O agro ganha lote, validade e FEFO na demonstração** (F11), com produto vencendo e lote vencido que a saída RECUSA — é o diferencial mais forte do ramo e hoje não aparece em tela nenhuma. **E o dono acrescentou o que não estava no plano: CAMPANHA SUGERIDA para o que vence em 90, 60 e 30 dias** — o sistema propõe a promoção em vez de esperar o produto vencer. Serve a qualquer ramo com validade, não só ao agro
- **As duas dívidas achadas no caminho entram na leva, não na revisão:** o teste intermitente de `fila.test.ts` (falha sob carga, passa isolado — teste que falha por relógio treina a ignorar vermelho) e `exigir_da_linha` com empresa nula (o helper confere por empresa e recusa a linha do GRUPO; contornado em `tabela_preco` com guarda própria, mas qualquer tabela futura com esse desenho repete o defeito)
- **As caronas do layout, CONFIRMADAS na leva** (11/08/2026): tela da trilha (`interno.trilha()`, pronta no banco e sem tela) · tela do consumo de IA (`interno.consumo_ia()`, idem — mostrará zero enquanto a IA estiver fora, mas passa a existir) · rejeição da SEFAZ traduzida na caixa de entrada (as rejeições são tabeladas e públicas: não depende do gateway) · e o N+1 do painel do operador. **Fica de fora a barra de comando que EXECUTA** — depende do LLMGateway, que custa por uso; o seam existe e não custa parado. E `testes/ux_balcao.test.ts` é reescrito no MESMO commit do layout: várias regras dele assumem o layout atual
- **Estado em 11/08/2026 — F6 a F12 CONSTRUÍDAS:** 1356 testes verdes, 107 migrações, 51 rotas percorridas nos três ramos. Devolução → kit/etiqueta/código interno → transferência → OS → comissão → agro → revisão de fim de onda
- **DEVOLUÇÃO (F6): dinheiro da gaveta OU crédito na ficha, o operador escolhe.** O teto é o que SAIU menos o que JÁ VOLTOU, contado ENTRE DOCUMENTOS — dois documentos que respeitam o teto sozinhos somam mais do que a venda tinha, e o estoque nasce do nada. O crédito CANCELA o título que `confirmar_devolucao` gera: são a mesma dívida, e os dois juntos fariam o dono ver a obrigação duas vezes. No balcão o caminho é dobrado (Lei 8: devolução é o raro), em tela própria para a que chega dias depois
- **KIT (F7): preço PRÓPRIO, sem saldo, explode ao confirmar.** O rateio escreve preço E DESCONTO, não só o preço: `total_cents` é derivado de `round(qtd × preço) − desconto`, então preço arredondado sozinho erra até (qtd−1) centavos e o balcão recusa a venda com "pagamentos somam 19998 e o total e 19999"
- **A FAIXA 2 do GS1 é da loja, e o layout dentro dela também.** Duas famílias disjuntas pelo SEGUNDO dígito: `2 0 SSSSSSSSSS D` é peça serializada, `2 CCCCCC VVVVV D` é etiqueta de balança. O modo da balança (peso ou preço) é PARÂMETRO por cliente — quem já tem balança não a reprograma, e chutar faz o PDV cobrar 1,20 kg de picanha como R$ 1,20, com as duas leituras plausíveis. **Valor zerado ou unidade que não fraciona valem UMA unidade**, e é isso que faz a etiqueta impressa pela própria loja funcionar nas duas configurações
- **`interno.ler_codigo` é o lugar ÚNICO da leitura.** A consulta vivia como literal no PDV e `/vender` tinha outra, só por `codigo`; a cura de 11/08 acrescentou o EAN lá e deixou duas buscas parecidas em dois arquivos, que é como se produz "no balcão acha e no orçamento não". As duas guardas que mediam o SQL literal passaram a medir a fonte
- **Etiqueta em PDF escrita à mão** (`lib/etiqueta.ts`), sem dependência nova. DUAS e não uma: gôndola leva PREÇO (é do cliente que escolhe) e produto leva BARRA (é do leitor do caixa) — no mesmo espaço a barra encolhe, o leitor erra e a fila espera. Código com dígito errado **não vira barra desenhada**: barra inventada é lida como outro produto
- **TRANSFERÊNCIA (F8) é DIRETA, sem trânsito.** Trânsito cria uma terceira posição de saldo que alguém tem de confirmar, e ninguém confirma: a mercadoria está na prateleira da outra loja há três dias e o sistema diz que está no caminhão. O CUSTO viaja junto — sem ele o destino entra a zero e a loja que recebe mostra margem infinita. **A assimetria é o desenho:** o destino pode ser de outra empresa do grupo (`interno.local_do_grupo`), a origem não — só se manda o que se tem
- **A OS (F9) tinha motor desde 04/08 e nenhuma tela.** Uma tela para os cinco tipos: eles são a mesma coisa com nomes diferentes, e o tipo muda o vocabulário, não a estrutura. Concluir faz TRÊS coisas juntas — baixa a peça, gera o título, fecha a OS — porque separadas produzem a OS concluída cujo material ninguém baixou. **`os.ver` e `os.gerenciar` NÃO EXISTIAM:** o módulo era vendável a R$ 149/mês e ninguém podia ser autorizado a usá-lo. O que não tem tela também não tem papel
- **COMISSÃO (F10) apurada no RECEBIMENTO, e o estorno é de graça.** O gancho é um gatilho em `movimento_financeiro` — não uma chamada dentro de `baixar_titulo` —, então vale para todo caminho que amortize. Desfazer a baixa escreve amortização NEGATIVA e a comissão negativa nasce sozinha: **não há uma linha escrita para o estorno**. Duas bases (recebido ou por produto); no modo produto o recebimento parcial é rateado pela composição da venda, senão o primeiro pagamento do crediário valeria mais que o último. Trocar a regra não reescreve o passado
- **AGRO (F11): alerta é do estoque, CAMPANHA é da venda.** A caixa de entrada diz "vence em X"; ela não responde o que empurrar esta semana e com quanto de desconto ainda dá lucro. Três faixas (90/60/30) mais o vencido, e o **desconto máximo é a margem sobre o custo DAQUELE LOTE** — a média do produto liquidaria um lote caro com base num barato comprado depois. Nota de produtor rural exige INSCRIÇÃO ESTADUAL. O produto com lote é o SÉTIMO do catálogo de propósito: `controla_lote` exige `lote_id` em toda saída, e marcar um dos seis ramificaria o movimento que os três ramos compartilham
- **A REVISÃO DE FIM DE ONDA (F12) achou CINCO VAZAMENTOS ENTRE CLIENTES**, todos reais, na primeira vez que a guarda nova rodou: `entregas_do_dia` (nome e ENDEREÇO), `mensagens` (a caixa de saída: e-mail e assunto), `funil_por_etapa` (o valor do funil de vendas), `aproveitamento_por_chapa` e `insucesso_por_motivo`. Quatro são RELATÓRIO, a um clique na tela. Invisíveis porque a demonstração teve um tenant só até 11/08 — a mesma razão do GED
- **`interno.exigir` NÃO PROTEGE, e `mensagens` é a prova definitiva:** ela já tinha `exigir('financeiro.ver')` na primeira linha e vazava do mesmo jeito. Ele confere a CAPACIDADE de quem chama, nunca o dono da linha
- **`testes/varredura.test.ts` fecha a OUTRA METADE da fronteira.** `fronteira.test.ts` mira definer que recebe ID; esta mira a que **não recebe id nenhum e varre**. Lê o catálogo, não uma lista de nomes — a lista do GED tinha sete nomes e a oitava função nasceria fora dela. Gatilho é excluído por FORMA (`returns trigger`), não por nome: ele opera sobre `new`/`old`, cuja escrita já passou pela RLS
- **`exigir_da_linha` aprendeu que `empresa_id` NULO é "do grupo".** Comparação com nulo é falsa em SQL, então ela recusava toda lista de preço do grupo para o próprio dono. `tabela_preco_minha` era o remendo e foi **dropada**; `local_do_grupo` fica, porque lá a linha TEM empresa e é deliberadamente de outra
- **A FILA é uma só e o Vitest roda arquivos em paralelo.** Era essa a intermitência de `fila.test.ts`: três suítes mexiam em `interno.evento_externo` e roubavam evento uma da outra, e "fila vazia devolve vazio" é afirmação global por natureza. Curado com **lock consultivo do Postgres** (`travarAFila` em `testes/db.ts`) — elas serializam entre si e seguem em paralelo com as outras oitenta e duas
- **`create or replace` com a lista de parâmetros trocada cria SOBRECARGA, não substituição.** Reescrevi `salvar_tabela_preco` numa ordem "mais natural" e a velha ficou viva, chamando a função que a mesma migração dropava. Quem pegou foi a guarda de fronteira, por um caminho que não era o dela — ao redefinir função existente, copie a assinatura do arquivo original
- **Estado em 12/08/2026 — a GRADE DE ALVOS nas telas de entrada:** 1362 testes verdes. A casca nova (trilho, barra de comando, ouro sobre ardósia) já valia para as 51 telas desde F3; o que faltava era o gesto aprovado no protótipo, e **medido antes de mexer ele existia em DUAS telas de cinquenta e uma**. As outras abriam com a ação escondida no canto do cabeçalho e uma tabela ocupando a tela — o layout que o dono cobrou por não ser o combinado
- **Cada alvo carrega NÚMERO, e alvo que mostra zero SOME.** Botão sem número não responde "o que eu faço agora", que é a pergunta que a grade existe para responder; e "faturar o que já saiu" não aparece quando não há nada a faturar, porque alvo que mostra zero todo dia é alvo que se aprende a não ver
- **TRÊS telas ficam fora da grade, com a razão escrita no próprio arquivo:** `/avisos`, `/mensagens` e `/trilha` são REGISTRO, não escolha — a ação mora em cada linha ("reenvie ESTA"), e um alvo no topo seria um botão que não sabe qual item resolver. Espalhar a grade por toda tela para o sistema parecer coerente é o oposto de coerência: é a forma virando enfeite
- **O financeiro passou a mostrar o mesmo R$ 19.734,00 TRÊS vezes** — no veredito, no alvo "Pagar o que já venceu" e no cartão "Vencido a pagar", este com exatamente as mesmas palavras do alvo. É o defeito que o painel do dono já teve. A divisão que ficou: o **veredito diagnostica**, o **alvo leva à ação**, e os **cartões guardam os totais** que nenhum dos dois carrega
- **`<form>` que embrulha um alvo vira célula do grid** e o botão sai com 210px num alvéolo de 251px, 79px de altura ao lado de um vizinho de 113px. A grade inteira desalinha, e é defeito PURAMENTE VISUAL — nenhum teste de comportamento o vê. `display: contents` no form, a mesma cura que os grupos do trilho precisaram
- **`testes/ux_balcao.test.ts` ganhou a lei da quarta peça, no mesmo commit** (era o combinado): a lista de seções sai da CASCA, não de uma lista escrita à mão, então seção nova passa a ser cobrada sozinha. E a exceção órfã quebra o build, como em `varredura.test.ts`
- **Estado em 12/08/2026 — o PREÇO DESCONGELADO:** 1381 testes verdes, 109 migrações. `interno.preco_catalogo` versiona plano e módulo (`exclude using gist` — dois preços vigentes na mesma data são impossíveis), `preco_de_catalogo(tipo, codigo, data)` é a fonte única e `mensalidade_de` é a conta inteira, num lugar só
- **Reajuste RETROATIVO é recusado** — reescreveria uma conta que o cliente já viu. E o reajuste pode ser AGENDADO: marcado para o dia primeiro, passa a valer sozinho. **A coluna do catálogo segue o preço de HOJE e o agendado não a toca**, senão a tela mostraria um preço que ainda não vale e alguém cobraria por ele
- **PREÇO DE FUNDADOR** em `assinatura.preco_travado_cents` e no item: quem entrou cedo não sente reajuste. **Nulo por padrão de propósito** — travar todo mundo na entrada faria a tabela virar enfeite, e o operador descobriria isso quando quisesse reajustar. O aviso passou a contar quem o reajuste ALCANÇA, separando os travados: contar todo mundo mentiria para cima exatamente na medida do sucesso comercial
- **`assinatura` tinha grant de TABELA INTEIRA** (desde a 0004) e a coluna nova nasceu gravável — armadilha 3 pela terceira vez. O agravante: eu tinha escrito um comentário ao lado afirmando o contrário. A lição não é sobre a armadilha, é sobre **afirmar sem medir**
- **Redefinir função existente é ler a versão VIGENTE, não a primeira que o grep acha.** Redefini `modulos_do_cliente` olhando o arquivo onde ela nasceu e apaguei o `empresa_id` que F1 acrescentou. Mesmo tropeço de F12, com outra roupa
- **`modulos_de_todos` e `modulos_do_cliente` têm o mesmo assunto e chamadores diferentes** — a primeira é a cura do N+1 e é ela que alimenta o painel. Estendi uma e esqueci a outra, e passou calado porque `m.travado` indefinido em JavaScript é só `false`: a tela mostrava o preço de tabela sem etiqueta nenhuma. **Só apareceu OLHANDO.** Virou lei em `preco_catalogo.test.ts`: as duas expõem as mesmas chaves e o mesmo preço travado
- **Estado em 12/08/2026 — 24 CAPACIDADES NÃO CONFERIAM NADA:** 1391 testes verdes, 110 migrações. A revisão mediu o catálogo contra o código e achou que, das 54, vinte e quatro não eram conferidas por ninguém — nem tela, nem função. **A tela de papéis deixava conceder e negar cada uma, e não mudava nada.** Não é vazamento entre clientes (a RLS isola): é DENTRO do cliente que qualquer usuário fazia o que o papel dele dizia que não podia
- **O buraco era a ESCRITA DIRETA.** A arquitetura diz que toda ação chama uma função, e é nela que `interno.exigir` mora — só que 19 tabelas são escritas por `insert`/`update` direto da aplicação, e ali não havia onde pôr a guarda. A cura é **gatilho**, nunca a chamada na ação de servidor: a ação é da aplicação, e um caminho novo (API, importação, correção manual) nasceria sem ela. `interno.capacidade_de_escrita` é o catálogo; registrar tabela nova é uma linha
- **13 conferidas · 5 aposentadas por duplicata · 6 por não haver o que guardar.** `pessoa.editar` e `produto.editar` eram `cadastro.editar`; `servicos.os` era `os.gerenciar`, que F9 criou sem ver que a antiga existia. Limite de crédito, regime tributário, frota, centro de custo, contrato de serviço e pedido de compra não têm NENHUMA escrita no sistema. **Aposentar é a regra do projeto de trás para frente** — o que não tem tela também não tem papel
- **As redefinições foram GERADAS de `pg_get_functiondef`**, não copiadas de arquivo. Copiar de arquivo já criou uma sobrecarga órfã e apagou uma coluna, nesta mesma leva. Gerar torna o erro impossível: a única diferença para o que estava no banco é a linha acrescentada
- **`interno.exigir` volta CALADO sem usuário na sessão** — é o caminho do worker e da semeadura, e é de propósito. O efeito colateral: as 1381 asserções existentes passaram intactas depois de pôr guarda em 15 funções e 19 tabelas, porque **nenhuma delas fixa usuário**. `testes/capacidade.test.ts` roda como GENTE, com dois usuários no mesmo cliente — um completo, outro só de consulta
- **A seguir, e agora é só isto:** faturamento e cobrança da própria plataforma, autosserviço de contratação — todos travados pelo BankingGateway. O número já está certo e explicável para quando esse dia chegar
- **Estado em 12/08/2026 — as LISTAS DE PREÇO ganharam tela:** 1401 testes verdes, 110 migrações, 53 rotas. O motor de F5 estava inteiro e inalcançável desde 11/08 — o preço saía sempre do `sku.preco_venda_cents`, que é o FALLBACK, e promoção, atacado e preço por empresa estavam construídos sem caminho. Mesmo padrão da OS
- **A tela mostra o que a regra precisava dizer em voz alta:** quantas listas valem hoje, **qual delas o balcão usa e por quê** (Lei 9), e "2 versões" ao lado do preço — que é "reajuste é linha nova" deixando de ser comentário de migração. **TIRAR é ENCERRAR**, nunca apagar: o preço sai da venda de amanhã e continua explicando a de ontem. E a lista que acabou continua na tela, marcada — sumir com ela faria quem procura "por que o desconto não aplica" não achar a resposta
- **`encerrar_preco` reclamava de item que ela mesma tinha acabado de tirar:** o caso do item que começou HOJE é resolvido pelo `delete` (intervalo invertido não passa no check) e a função só olhava a contagem do `update`. Duas contagens, não uma — pego pelo teste
- **Dívidas que ficam, medidas em 12/08:** `responder_pedido_modulo` sem tela (o cliente pede o módulo e ninguém responde) · `pedido_compra` sem tela · `marca` e `recusa_mercadoria` sem tela · 16 enums que a aplicação nomeia e `lib/rotulo.ts` não traduz
- **Estado em 12/08/2026 — as CINCO DÍVIDAS MEDIDAS, fechadas:** 1431 testes verdes, 112 migrações, 55 rotas. Traduções dos 17 enums · pedido de módulo com resposta · pedido de compra · recusa de mercadoria · marca
- **"ATENDIDO" NÃO CONTRATAVA NADA.** `responder_pedido_modulo` marcava o pedido e voltava; o módulo continuava fora de `assinatura_item`, logo fora do menu. O cliente leria "contratado" e seguiria batendo em `/sem-modulo` — o pior chamado que o projeto reconhece ("eu paguei e não aparece"), agravado por o sistema ter AFIRMADO que atendeu. Agora contrata na MESMA transação: duas chamadas separadas na tela deixariam a janela em que a primeira passa e a segunda falha
- **A função estava QUEBRADA desde que nasceu, e ninguém soube:** com `search_path = ''` os dois literais do `case` são desconhecidos, o resultado vira `text` e o Postgres não acha conversão para um enum fora do caminho. Nenhuma tela e nenhum teste a chamavam — a definição de código inalcançável. Só apareceu ao lhe dar tela
- **A resposta é a PRIMEIRA notificação do sistema a preencher `acao_rotulo`/`acao_destino`** — as colunas existiam vazias desde 03/08 e obrigavam `lib/aviso.ts` a adivinhar pelo tipo. Aqui o banco sabe a rota exata (`modulo_secao`), então quem manda é ele. O insert é DIRETO e não via `interno.notificar()`: aquela lê `current_tenant()`, e quem responde é o operador — o aviso iria para a caixa de quem já sabe. E `historico_de_pedidos_de_modulo()` existe porque recusado sumia: `meus_pedidos_de_modulo()` só devolve ABERTOS, e o cliente voltava na semana seguinte vendo o botão como se ninguém tivesse respondido
- **PEDIDO DE COMPRA: o pedido é o que se PEDIU, a entrada é o que CHEGOU.** Os dois divergem o tempo todo, e é essa diferença que some quando se registra só a entrada. Só muda em rascunho — prova que se edita não prova nada. O mesmo SKU duas vezes SOMA (duas linhas é o que o fornecedor separa errado: lê "10" e "5" e manda 10). `entrada_do_pedido` copia os itens e nasce em RASCUNHO: fechar o pedido ali diria que a mercadoria entrou no instante em que alguém abriu a tela — quem recebe é `confirmar_entrada`, que já fechava o pedido desde 04/08. ATRASADO é derivado do relógio e não entra no enum, como o "atrasado" de `estadoDoTitulo()`
- **RECUSA: o único fato de compra que não vira estoque nem dívida.** Registrar como entrada cancelada faria o saldo subir e descer por mercadoria que nunca esteve na loja, e o custo médio guardaria a marca de uma compra que não houve. Não se recusa o que já foi conferido — aí o caminho é a devolução, e os dois fatos juntos deixariam quem lê escolhendo em qual acreditar
- **MARCA sem tela própria, e é decisão.** `produto.marca_id` apontava para uma tabela que ninguém alimentava. "Cadastro de marcas" é a saída errada: ninguém abre o sistema para cadastrar marca. Ela nasce digitada no cadastro do produto, com `datalist` das existentes e `marca_de()` casando sem diferenciar maiúsculas. O preço é a gêmea por digitação, e a lista com CONTAGEM é o que a denuncia — uma marca com um produto ao lado da que tem quarenta
- **A CONFERÊNCIA DA NOTA assumia XML, e mentia para quem não tinha.** Entrada nascida de pedido (ou nota de produtor, desde F11) não tem `documento_fiscal`; o `coalesce(..., 0)` fazia a tela dizer "R$ 1.316,00 a mais que a nota" contra um declarado que nunca existiu, pintando de vermelho uma conferência perfeita. E a lista contava só item de XML, mostrando "0 itens" numa nota com item dentro. **Nenhum teste pegou nenhum dos dois: os dois só aparecem ao abrir a tela**
- **Percorrido com dado real:** montar → enviar → receber → conferir → confirmar, o pedido fechando sozinho e o "chegou" batendo com o pedido; recusa registrada e listada; marca gravada e contada; e o pedido de módulo do cliente até o aviso com botão que abre `/os`. Dois defeitos meus achados assim — `valor()` no lugar de `quantidade()` fazia 40 aparecer como 0,40, e o veredito lia "R$ 0,00 pedidos e ainda não recebidos" com zero pedidos
- **Backtick dentro de template literal fecha a string.** Comentar SQL com `` `documento_fiscal` `` dentro de uma consulta em crase quebrou o build com erro de vírgula três linhas adiante — o `tsc` aponta o efeito, não a causa
- **Estado em 12/08/2026 — a REVISÃO GERAL e as quatro frentes que ela abriu:** 1470 testes verdes, 124 migrações, 58 rotas. A medição contra o catálogo achou **44 funções de `interno` (de 341) sem nenhum chamador** — motor construído, sem porta. Foram atacadas na ordem: conciliação → crediário → segmento/chave de API → o relógio
- **QUATRO ACESSOS, um por variante** (12/08/2026): `marmoraria@`, `varejo@` e `rural@` para os ramos, `raphael@` para a plataforma. O operador ganhou porta própria em `papel_de_tela()` e abre em `/operador` — sem ela caía em `dono` e abria no painel de vendas de um cliente qualquer. Ele tem vínculo com as TRÊS demonstrações e **não** um tenant só dele: toda sessão carrega tenant (`fixar_contexto` recusa nulo, é o selo HMAC que sustenta a RLS), e um quarto tenant "Plataforma" apareceria na carteira do próprio painel como um cliente que não é cliente, mentindo no MRR
- **A guarda "todo papel tem porta" lia o TEXTO da migração** e passou a ler o catálogo: ela procurava `create type` no arquivo, então valor acrescentado por `alter type` — o único jeito, porque `add value` não convive com o uso na mesma transação — ficava fora da conta e ela seguia verde sem cobrir o papel novo
- **CONCILIAÇÃO: o motor estava inteiro desde a Onda 1 lendo uma tabela com ZERO linhas.** Não havia caminho para pôr uma lá — Open Finance depende de convênio, webhook depende de contrato com cada banco. E **o site público já dizia "a conciliação já funciona com o extrato importado"**: era a única promessa do produto que nenhuma tela entregava. `lib/ofx.ts` é leitor próprio, sem dependência
- **OFX 1.x é SGML, não XML: a tag de valor NÃO fecha**, e a folha fecha sozinha quando a próxima abre. Escrevi errado da primeira vez e o teste pegou — sem isso o `DTPOSTED` virava filho do `TRNTYPE` e o `FITID` sumia, com o arquivo inteiro sendo recusado por "lançamento sem FITID". O valor vem pelo TEXTO, nunca `Number(x)*100`: 1316.35×100 dá 131634,99999 e um centavo a menos faz a automática recusar o que batia
- **FITID é a chave, e é o que torna reimportar inofensivo.** Arquivo sem FITID é recusado INTEIRO em vez de ganhar id inventado: chave sintética faria a segunda importação do mesmo mês entrar de novo, e extrato dobrado não bate com o do banco em nada
- **`lancar_direto` existe porque sem ele a fila NÃO FECHAVA para tarifa.** O motor já sabia lançar direto por REGRA desde a Onda 1, e criar a regra só era possível por `conciliar_manual`, que exige ao menos um título aplicado — tarifa não tem título. A fila fechava para tudo menos para o caso que a regra existe para resolver. **Achado ao percorrer a tela com extrato de verdade, não em teste.** A regra aprende pela DESCRIÇÃO e não pelo valor (tarifa muda de mês para mês), APARECE na tela com quantas vezes serviu, e desligar DESLIGA em vez de apagar
- **CREDIÁRIO: o módulo inteiro tinha motor e nenhuma porta, e havia um nó no meio** — `abrir_crediario` RECUSA cliente sem limite, e NADA escrevia em `limite_credito` (zero linhas nas três demonstrações). Mesmo com tela de venda a prazo, toda venda seria recusada. Limite novo é LINHA NOVA; **zero é DECISÃO ("só à vista") e é diferente de sem limite ("ninguém olhou")**, e o banco recusa os dois com frases diferentes
- **Fechar a vigência em `hoje − 1` invertia o intervalo** quando a linha anterior tinha nascido no mesmo dia, e o check recusava — mesmo tropeço de `encerrar_preco` nas listas de preço. Para a linha de hoje, fim = início: as duas ficam vigentes e `criado_em desc` desempata
- **`carteira_de_credito` nasceu VAZANDO** — definer varrendo `pessoa` e `titulo` sem citar contexto, devolveria nome, CPF, dívida e atraso dos clientes de todos os assinantes na primeira tela do módulo. `testes/varredura.test.ts` quebrou o build antes de sair da máquina
- **`compras.pedido` voltou do armário**: as funções do pedido de compra nasceram exigindo `compras.entrada`, que é a capacidade de CONFIRMAR nota — a que move estoque e cria dívida. Quem monta pedido não precisa dela
- **O CONTRATO do crediário estava em jargão de máquina:** "Confisso a divida de 76700 centavos". Grafia errada, unidade de máquina e sem acento — num documento legal, que é a **sexta ocorrência** da classe curada em 09/08 ("texto de tela escrito pelo BANCO"). `dinheiro_br` devolve só o número, então o "R$" vem à mão: em contrato, valor sem moeda é ambíguo
- **SEGMENTO E CHAVE DE API — as duas que travavam onboarding.** O ramo de uma empresa só se definia por script; sem ele o sistema fala "produto" e "produção" para quem diz chapa e corte. E `/api/v1/*` estava no ar sem caminho para emitir credencial. **A chave aparece UMA vez** (o banco guarda o hash; uma tela "ver de novo" exigiria guardar o segredo). A lista de capacidades sai de `capacidades_que_posso_delegar()`, que devolve só o que QUEM CHAMA tem — `criar_chave_api` confere que a capacidade existe, não que o criador a possua
- **O RELÓGIO: cinco rotinas construídas e nunca chamadas.** `alertar_vencimentos` (F11, com tela e notificação, **nunca disparou uma vez**), `gerar_titulos_recorrentes`, `conferir_limites`, `liberar_reservas_vencidas` e o expurgo do GED. **Silêncio é o pior sintoma que existe: ninguém descobre que uma rotina não roda, porque o efeito dela é uma coisa APARECER.** `lib/rotinas.ts` + `?diario=1` + agendamento às 07:00 BRT — e não junto com o expurgo das 04:00, porque alerta de validade tem de estar na caixa de entrada quando a pessoa abre o sistema
- **A lista de empresas do worker vem de FUNÇÃO** (`empresas_para_rotina`): sem contexto a RLS filtra `empresa` inteira, e o laço rodaria zero vezes em silêncio — que é justamente o defeito que as rotinas curam. A primeira versão devolveu lista vazia e só o teste viu
- **E o primeiro disparo de `alertar_vencimentos` revelou DOIS defeitos de fronteira na mesma função.** (1) A dedupe consultava `notificacao` de TODOS os clientes: dois assinantes com lote de código igual — "L001", "A1" — calavam um ao outro, e o lote vencia sem aviso nenhum. (2) Pior: a consulta principal varria `estoque_lote` inteira e agrupava por `l.codigo`, que não é único entre clientes — **o saldo de um concorrente somava no aviso do outro** ("ainda há 80 L" onde havia 40). O recorte é por EMPRESA e não só por tenant, porque a matriz receberia o aviso do armazém da filial
- **Nenhuma guarda pega essa forma:** `varredura.test.ts` lista `alertar_vencimentos` como quem varre de propósito (o worker roda sem sessão), e a exceção é legítima para o laço externo — o que faltava era o recorte DEPOIS de o contexto ser fixado. Quem pegou foi o teste com DOIS clientes, que é a regra do projeto: "um cliente só no banco não prova isolamento"
- **A sétima ocorrência de texto escrito pelo banco:** o aviso dizia "saldo 4.0000. Depois do vencimento a saida e bloqueada" — `numeric(18,4)` cru e sem acento. Invisível por um motivo novo: a função nunca tinha sido CHAMADA
- **Estado em 12/08/2026 — o RESTO das sem chamador, de 31 para 16:** 1476 testes verdes, 129 migrações, 61 rotas. Nem toda função sem chamador é dívida de tela: **sete eram DUPLICATA ou SEGUNDA VERDADE**, e ali o certo é apagar, não construir
- **Aposentadas por duplicata:** `fila_do_dia` (→ `agenda_de`, que veio em F9 com intervalo de datas E com `exigir('os.ver')`) · `modulos_do_cliente` (→ `modulos_de_todos`, que a tela usa — manter as duas já custou uma coluna esquecida) · `notas_pendentes` (filtrava por `observacao like 'Recebida do XML%'`, prefixo de texto escrito por outra função, para responder o que o STATUS responde)
- **Aposentadas por SEGUNDA VERDADE — a mesma pergunta com resposta diferente da que o sistema usa:** `margem_do_sku` (margem sobre o custo do CADASTRO; a tela usa o custo MÉDIO e o comentário dela diz que o outro "diverge do real") · `resumo_de_vencimento` (agrupava o que a tela já agrupa) · `percentual_da_venda` (a apuração real é no gatilho, sobre o recebimento — segundo cálculo é onde nasce "a comissão da tela não bate com a paga") · `historico_de_preco` (o `catalogo_do_operador` já traz o histórico; ela tinha o MOTIVO a mais, que **passou para o histórico embutido** antes de ela sair)
- **DRE GERENCIAL estava fora do catálogo de relatórios** — construído, e inexistente para quem abre `/relatorios`. Pôr o código no catálogo não bastou: a tela mostrou `[object Object]`, porque o catálogo monta TABELA e a função devolve um `jsonb` de doze chaves. `dre_gerencial_linhas` adapta — uma verdade, dois formatos —, com a ordem escrita, porque DRE fora de ordem não é DRE
- **PORTABILIDADE (LGPD) sem botão:** `exportar_meu_tenant` tinha suíte própria desde a Onda 1 e nenhuma tela. Direito que só existe como chamada de banco não é direito. `/cadastros/meus-dados` baixa o JSON por rota GET (ação de servidor devolve navegação, não `content-disposition`) e **não apaga**: `apagar_tenant` continua sem botão de propósito
- **`registrar_consulta_bureau` NÃO CONFERIA NADA** — nem capacidade nem se a pessoa é deste cliente. Ela grava score e restrições, o insumo da decisão de crédito, e qualquer usuário do balcão inventava 900 para um conhecido. `fronteira.test.ts` não a pegou porque a linha alvo chega por parâmetro e ela escreve em OUTRA tabela. E a tela diz em voz alta que **o sistema não consulta bureau nenhum**: fingir que consulta faria um número digitado ter cara de automático
- **O CRÉDITO do cliente entrou na carteira** (devolução em crédito na ficha): é dinheiro dele que está com a loja, e aprovar limite sem olhá-lo é negar fiado a quem já tem saldo lá dentro
- **A CONFORMIDADE DO XML ganhou painel:** das notas AUTORIZADAS, quais não têm o arquivo guardado, ou têm sem hash, ou sem prazo. São cinco anos de obrigação, e a pergunta só costuma aparecer quando a fiscalização faz — aí não há o que fazer. E `anexos_de` passou a ser chamada pela VENDA: o documento só aparecia na lista geral, e quem abre a venda para conferir um problema é quem precisa do comprovante
- **ID TORTO NA URL DERRUBAVA A TELA COM 500** — achado sem querer, usando `?v=2` como quebra-cache. Toda rota `[id]` passa o pedaço da URL para uma consulta com `$1::uuid`, e `/vender/abc` virava "a server error occurred", indistinguível de sistema quebrado. Curado em `consultar()`, **num lugar só**, então vale também para as telas que ainda não existem. **`executar()` NÃO herda**: numa ação o mesmo erro precisa chegar ao `falhar()` e virar frase no formulário, e não página inexistente. `testes/telas.test.ts` trava os dois lados
- **Na tela da lista de preço, duas perguntas que não tinham resposta:** o histórico por item (ela dizia "3 versões" e não mostrava quais — a regra "reajuste é linha nova" aparecia como número, não como fato) e **qual preço o balcão usa**, porque uma lista pode existir, valer hoje e mesmo assim perder para outra de prioridade maior. "Por que o desconto não aplica" não tinha resposta na tela que deveria dá-la
- **Sobram 16 de 350 sem chamador, e o que sobra tem razão:** três dependem de terceiro (`cancelar_documento`, `rejeitar_documento` e `simular_repasse_tributario` — SEFAZ e contrato) · quatro são helper de crediário/GED que a carteira passou a responder por consulta própria (`credito_de`, `crediario_cobravel`, `bureau_vigente`) · e o resto é tela que ainda não existe: roteiro de entrega do dia (`entregas_do_dia`), reserva de peça (`reservar_item`/`liberar_item`/`desprogramar`), FEFO sugerido (`sugerir_fefo`), fechamento contábil (`apurar_competencia`), Fator R (`fator_r_automatico`), itens do evento trabalhista (`itens_do_evento`), resolver evento de integração (`resolver_evento`) e vocabulário por segmento (`salvar_padrao_segmento`)
- **A seguir:** eSocial via fornecedor, local-first do PDV (adiado pelo dono até a maturidade do projeto)
- **Sem dependência nova, de propósito:** parser de XML (`lib/xml.ts`), leitor de NF-e (`lib/nfe.ts`), escritor de XLSX (`lib/exportar.ts`), leitor de CSV (`lib/importar.ts`), conferência de upload (`lib/arquivo.ts`)

## Regras do usuário

- Responder sempre em **português brasileiro**.
- **NÃO misturar projetos.** Este é isolado do MMX, do APP Beleza e de qualquer outro. Nunca citar dados de outro projeto aqui.
- **Política de entrega:** sanar todas as dívidas antes de fechar uma etapa, sem deixar remendos. Backup depois que estiver limpo.
- Ao propor uma estratégia, reavaliar criticamente antes de implementar — o usuário cobra isso, e já retirei propostas por causa dessa cobrança.

## Antes de mexer no banco

Quatro armadilhas do Postgres que já custaram caro aqui e voltam a cada migração nova:

1. **`CREATE FUNCTION` concede EXECUTE a `PUBLIC`.** Um `revoke ... from <papel>` não tira nada. Desde 07/08/2026 a cura é uma linha no fim do arquivo: `select interno.fechar_para_public();`
2. **`ALTER DEFAULT PRIVILEGES` (migrações 0004/0005) faz toda tabela nova em `public` nascer gravável** pelos papéis da aplicação. Todo migration novo precisa do bloco explícito de `revoke`/`grant`.
3. **`revoke update (coluna)` não restringe um grant de tabela inteira.** Revogue a tabela e conceda coluna a coluna.
4. **`security definer` roda como o dono, e o dono tem `BYPASSRLS`: dentro dessas funções a RLS NÃO filtra nada.** (07/08/2026) Um `where id = $1` numa função definer alcança a linha de **qualquer cliente**. Toda função que recebe id de linha de negócio precisa de `perform interno.exigir_da_linha('<tabela>', <p_id>)` no topo. **`interno.exigir()` NÃO protege** — ele confere capacidade no contexto de quem chama, nunca sobre a linha alvo: um usuário legítimo passa por ele e em seguida alcança a linha de qualquer outro. Fechada em 08/08/2026 (81 → 0); `testes/fronteira.test.ts` quebra o build se voltar.

E: quem faz `revoke execute on all functions in schema interno from public` derruba os grants das migrações anteriores — reabra a lista inteira no fim do arquivo.

**Um cliente só no banco não prova isolamento.** A armadilha 4 passou por 19 testes verdes porque só existia um tenant com plano de contas — `select ... into` pegava a linha certa por sorte. Suíte que fala de fronteira precisa de **dois** clientes com a mesma configuração, e o teste tem de ser determinista (evento que só o vizinho tem), não "confere de quem veio a linha".

**`to_char` com `G` e `D` segue o `lc_numeric` do servidor, que aqui é `C`** — mensagem de dinheiro saía "100.00" em tela portuguesa. Use `interno.dinheiro_br(cents)`, que usa separadores literais.

Rotina ao fechar uma etapa: `npx.cmd supabase db reset --no-seed` → `npx.cmd tsc --noEmit` → `npm test` → commit → push → CI → `npx.cmd supabase db push`.

**A guarda olha INSERT e UPDATE** (desde 06/08/2026). Olhava só UPDATE, e foi assim por toda a Onda 1: as 67 colunas derivadas estavam protegidas contra alteração e abertas para criação — que é a metade que importa, porque o número errado nasce com a linha. Gravei R$ 999.999,99 no total de uma venda sem item nenhum com a guarda verde. Ao abrir o olho ela acusou **128 violações**, incluindo a prova do aceite do crediário e o `sha256` do GED.

Daí saíram dois conceitos que o registro não separava:

- **`momento = 'nunca'`** — calculada por nós (`venda.total_cents`). A aplicação não escreve em tempo nenhum.
- **`momento = 'na_criacao'`** — fato externo gravado uma vez (`movimento_bancario.valor_cents`, `documento_fiscal.chave`). Quem importa precisa gravar; ninguém edita depois. São **5** e a lista é conferida por teste — `na_criacao` é exceção justificada, não porta de saída.

E **`interno.tabela_historico`**: livro-razão não aceita UPDATE nem DELETE. `movimento_bancario` aceitava DELETE e ninguém via, porque nenhuma regra perguntava.

**Coluna derivada entra em `interno.coluna_derivada`.** A guarda `interno.auditar_padroes()` roda no teste e quebra o build se alguma delas estiver gravável — foi ela que pegou 18 colunas abertas no fim da Onda 1, incluindo a prova do aceite do crediário. Se `testes/padroes.test.ts` falhar, ou faltou um `revoke` na migração nova, ou a coluna derivada não foi registrada. Não "ajustar o teste".

**Revisão de fim de onda** (prática adotada em 05/08/2026): antes de fechar cada onda, rodar a auditoria, sondar o que ela ainda não cobre e transformar todo achado repetível em regra da guarda.

**A revisão do banco não cobre as telas** (aprendido em 06/08/2026). A auditoria olha esquema; ela não abre uma página. Percorrer as 22 rotas com dado real achou 8 defeitos que 521 testes não pegavam — inclusive uma sessão que sobrevivia à revogação de acesso e dois relatórios que nunca funcionaram. Duas regras que saíram daí:

1. **Semear um cenário com movimento antes de conferir tela.** Tela vazia esconde defeito: formatação de dinheiro, soma, ordenação e faixa de atraso só erram quando há linha. O cenário está em `scripts/cenario.ts` e é semeado pelas MESMAS funções que a tela usa — `insert` no que é derivado cria um banco que a aplicação nunca produziria.
2. **Guarda que nunca falhou não prova nada.** Toda guarda nova é verificada quebrando o código de propósito antes de consertar. Uma delas "falhou" por erro meu de harness e precisou ser refeita — falha por engano do teste não é prova.

## Arquitetura que não se desfaz

- **Contexto selado, não JWT.** `interno.current_tenant()` só aceita tenant acompanhado de selo HMAC cujo segredo mora em `interno`. Claim de JWT não é fonte aceita.
- **RLS com `force row level security`** em toda tabela de negócio, nunca filtro na aplicação. `interno.verificar_isolamento()` quebra o build se uma tabela nova nascer desprotegida.
- **Dado derivado não se escreve.** Saldo deriva do movimento; total da venda, dos itens.

## Stack

Next.js 16 + React 19 + Supabase + RLS. Dinheiro em centavos inteiros; quantidade em `numeric(18,4)`. Pooler de transação (6543) para a app, pooler de sessão (5432) para os testes.

**`DATABASE_URL` não conecta sozinha.** A senha vem separada em `DATABASE_PASSWORD` e é embutida na URL por `lib/conexao.ts`, codificada — senha de gerenciador tem `#`, `@` ou `%`, que quebrariam a URL. Faltando a variável, o `pg` recebe `undefined` e devolve `SASL: client password must be a string`. Já derrubou a semeadura na nuvem e o primeiro deploy na Vercel. Ao configurar ambiente novo, as duas variáveis andam juntas — e a porta é 6543, não a 5432 do `.env.nuvem`.

**Mensagem de erro em ação de servidor usa `falhar()` de `lib/tela`**, nunca `redirect('...?erro=')` na mão. Sem `revalidatePath` antes, o Next devolve o RSC que já tinha da rota: a URL muda e a tela não — o erro some, ou herda o aviso verde da tentativa anterior e diz "salvo" depois de uma recusa. `testes/mensagens.test.ts` quebra o build se alguém voltar a montar na mão.
